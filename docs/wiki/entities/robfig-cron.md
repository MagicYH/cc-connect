# robfig/cron/v3

Go cron library. One of only two external dependencies allowed in `core/`.

**In this project**: Used in `core/cron.go` for cron job scheduling and execution.

**Cross-references**: [Dependency Direction](../concepts/dependency-direction.md)
