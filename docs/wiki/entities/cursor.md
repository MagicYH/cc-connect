# Cursor

Cursor AI coding editor's agent mode.

**In this project**: The `agent/cursor/` package (1,301 lines) implements the adapter. Key files: `cursor.go`, `session.go`.

**Cross-references**: [Plugin Architecture](../concepts/plugin-architecture.md)
