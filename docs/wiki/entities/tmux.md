# tmux

tmux terminal multiplexer used as an agent adapter. Enables interacting with any CLI tool through a tmux session.

**In this project**: The `agent/tmux/` package (764 lines) implements the adapter. Key files: `tmux.go`, `session.go`.

**Cross-references**: [Plugin Architecture](../concepts/plugin-architecture.md)
