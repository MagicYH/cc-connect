# Zustand

Zustand (v5.0.5) is a lightweight state management library used in cc-connect's web dashboard frontend.

It manages application state for the React-based dashboard, providing a simpler alternative to Redux with minimal boilerplate.

Cross-references: [tech-stack](../sources/tech-stack.md), [react](./react.md)
