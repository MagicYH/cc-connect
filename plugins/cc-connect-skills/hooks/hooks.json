{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/stop-mention-reminder.sh",
            "id": "ccs:stop:mention-reminder",
            "description": "Remind the bot to use Feishu <at> tags when sending messages to specific bots instead of plain @name"
          }
        ]
      }
    ]
  }
}
